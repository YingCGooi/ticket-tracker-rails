<nav>
  <h2>Ticket Tracker</h2>  
  <%= link_to 'Projects', projects_path %><span></span>
  <%= link_to 'Tickets', tickets_path %><span></span>
  <%= link_to 'Tags' %>
</nav>

<section class="align-right">
  <% if logged_in? %>
    <small><em>Logged in as <%= current_user.name %></em></small>
    <br/>
    <%= link_to 'Log out', logout_path %>
  <% else %> 
    <%= link_to 'Log in', login_path %><span></span>  
    <%= link_to 'Sign up', new_user_path, class: "btn" %>
  <% end %>
</section>